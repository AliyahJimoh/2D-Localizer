# You can edit the files themselves in the inputs folder

beacons: # Beacon (range sensor) positions
 "inputs/beacons.csv"

FM: # Fiducial marker placement
            #x, y, theta(radians)
    fm_map: "inputs/fiducial_map.csv" # FM from the map's frame (T_mf)
    
map: 
    name: "inputs/factorylayout3.jpg" # Name of your image
    size: [60, 45] # length (x-axis) and width (y-axis) in meters

trajectory: "inputs/trajectory_curvy.csv"

sensor_data:
    variances: 0.5