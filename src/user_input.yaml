beacons: # Beacon (range sensor) positions
 "beacons.csv"

FM: # Fiducial marker placement
            #x, y, theta(radians)
    fm_map: "fiducial_map.csv" # FM from the map's frame (T_mf)
    
map: 
    name: "factorylayout3.jpg" # Name of your image
    size: [60, 45] # length (x-axis) and width (y-axis) in meters

trajectory: "trajectory_curvy.csv"

sensor_data:
    variances: 0.5